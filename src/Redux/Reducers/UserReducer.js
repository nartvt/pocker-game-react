let initialState = {
  credentials: null,
  profiles: [],
  users: []
}

const UserReducer = (state=initialState,action)=>{
  switch(action.type){
    default: return state;
  }
}

export default UserReducer;